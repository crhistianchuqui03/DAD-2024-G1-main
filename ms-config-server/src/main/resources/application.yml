server:
  port: 7071


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/crhistianchuqui03/DAD-2024-G1-main.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
          #username:
          #password:
  security:
    user:
      name: root
      password: 123456